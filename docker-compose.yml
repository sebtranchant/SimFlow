version: "3.8"

services:
  simflow:
    build: .
    image: simflow:latest
    container_name: simflow
    volumes:
      # map the Config folder from the solution root into the container
      - ./Config:/SimFlow/Config
    # you can override ENTRYPOINT or command here if needed
    # command: ["dotnet", "SimFlow.dll"]
